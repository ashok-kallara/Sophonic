---
description: Guided setup — check what's configured and fill the gaps (vault, Google, Slack, Zoom, GitLab).
---

Use the `setup` skill: run the doctor probe, tell me what's configured and what needs
attention, then walk me through fixing each gap (asking me for values, handing secret
and browser/Keychain steps to my own terminal). Re-check when done. Extra context:
$ARGUMENTS
