---
description: Show today's calendar, tasks due today, and yesterday's unfinished tasks.
---

Show me today at a glance:
1. Today's calendar events (use the `gcal` skill; skip if Google isn't configured).
2. Tasks due today and any overdue tasks (use the `obsidian` skill to scan the vault).
3. Yesterday's unfinished tasks that haven't rolled over yet.

Read-only — just report; don't modify the vault unless I ask.
