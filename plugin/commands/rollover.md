---
description: Roll yesterday's incomplete tasks into today's daily note
---

Run `sophonic rollover` via the Bash tool and report how many tasks were carried
over into today's daily note, listing them. It is idempotent, so it is safe to run
more than once.
