---
description: Morning briefing — today's calendar, tasks due today, and yesterday's incomplete tasks
---

Run `sophonic today` via the Bash tool and present the output as a concise morning
briefing: calendar events first, then tasks due today, then anything left over from
yesterday. If any section reports an auth error, tell the user which
`sophonic auth …` command to run.
